<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Welcome page
  body
    h1 Hello world 
      h2 #{ message }

      a(href=Helper.route("home.login")) Login
      a(href=Helper.route("home.logout")) Logout