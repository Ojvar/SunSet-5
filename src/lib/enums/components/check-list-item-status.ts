/**
 * Check list item status enum
 */
export enum CheckListItemStatusEnum {
    ACCEPTED = "ACCEPTED",
    INQUIRY = "INQUIRY",
    NONE = "NONE",
    PENDING = "PENDING",
    REGISTERED = "REGISTERED",
    REJECTED = "REJECTED",
}
